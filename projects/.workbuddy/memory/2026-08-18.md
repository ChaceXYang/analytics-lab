# 2026-08-18 工作记录

## H&M 项目 2.2 同期群留存

- 下午 13:34 进入 2.2（同期群留存矩阵），拆成 4 小步：2.2a 首购月 / 2.2b 每 cohort 每月的活跃人数 / 2.2c cohort 总人数 / 2.2d 月份差+留存率。
- 2.2a 用户问"表 select 哪些列，对表没感觉"——已教核心直觉：**建表前先问"一行代表什么"**。
  - monthly_sales 一行 = 一个客户的一个月
  - first_buy 一行 = 一个客户（的首购月）→ 两列：customer_id + cohort_month
  - 这个直觉贯穿 2.2 的三个 CTE（first_buy/activity/sizes），后续 2.2b/c 还要复用。
- 用户对"表结构/数据建模"缺乏直觉，后续建表时继续用"一行代表什么"引导。
- 澄清 JOIN = INNER JOIN（用户学的是 ANSI 标准 INNER JOIN 显式写法）。
- **2.2b 完成**（14:55）：activity 表跑通（cohort_month × month × active_users）。2018-09 cohort 首月 140340 人 → 第1月 61551（留存率约44%，断崖式下跌），之后缓慢衰减。观察：首月留存断崖是典型电商特征。
- 进入 2.2c：cohort_size（COUNT(*) FROM first_buy GROUP BY cohort_month）。验证点：2018-09 cohort_size 应 = 140340（= activity 首月 active_users，因首购月必然活跃）。
- **2.2c 完成**（15:00）：sizes 表跑通，2018-09=140340（与 activity 对上）。观察：2018-10 首购最多（212669），可能有营销活动/季节因素。
- 进入 2.2d（最后一步）：month_n（substr 提取年月 + 月份差公式）+ retention_pct（active_users/cohort_size，注意用 100.0 浮点除）。substr 是 SQLite 函数，pandas 书/SQL必知必会 都没有，需直接教。
- **2.2 完整完成**（15:22）：cohort_retention 表跑通。2018-09 cohort 留存：首月 100% → 第1月 43.86%（断崖），第24月 24.30%。季节性回升（2019年3-6月峰值约42.7%，换季购物驱动）。
- 三个业务洞察（已给用户）：① 首月断崖 → 新客 30 天内建复购钩子；② 两年后仍 24% → 腰部客户值得经营；③ 3-6 月换季回升 → 营销踩换季节点。
- 2.2 审查过程用户踩的坑：JOIN 用隐式逗号/漏 group by month；active_users 误用 CASE WHEN（分类 vs 计数混淆）；cutomer_id typo；DROP+CREATE 拼接（conn.execute 单语句）；SQL 注释 `-` vs `--`；`create table if exists` 语法错。
- **下一步：2.3 复购结构**（一次性 vs 复购客群占比与收入贡献，较简单）。
- **重要发现（影响 2.4）**：monthly_sales 是"客户×月"聚合表，**没有 article_id/品类维度**。2.4 品类帕累托需要两个前置：① articles.csv 写进库；② 重新分块读 transactions（3.7GB）聚合成商品/品类粒度。所以 2.4 实际工作量 ≈ 1 小时（含分块处理），不是"简单一步"。
- 16:11 用户问"2.3/2.4 快吗"后继续。2.3 预计 20 分钟内（基于现有表），2.4 需补前置。
- **2.3 完成**（16:32）：复购结构——一次性客户 49.2万(36.08%) 贡献 **5.76%** 收入；复购客户 87.1万(63.92%) 贡献 **94.24%** 收入。
- **核心洞察：生意主要靠老客复购**（64% 人贡献 94% 收入），直接回答项目核心问题。一次性客户 36% 人只贡献 5.8% → 拉新要精准 + 首单后 30 天推复购。与 2.2 首月断崖互相印证，面试可串联。
- 2.3 过程用户又踩中文标点（全角逗号 `，`）+ active_month/active_months 拼写。中文标点坑已三次（引号/句号/逗号），已给自查习惯。
- **下一步：先 2.5 沉睡客群（快，基于 rfm_base），2.4 品类帕累托放最后（需前置）**。
