# 2026-08-14 工作记录

## H&M 客户复购分析项目（P0）启动

- 数据已从桌面挪进 `hm-customer-retention-rfm/data/`：articles.csv(36MB)、customers.csv(207MB)、transactions_train.csv(3.49GB)。sample_submission.csv(270MB) 和 images/ 留在桌面（项目不用）。
- `git status --ignored` 确认 3 个 csv 显示 `!!`（已被忽略），未进版本库。git 根是 `E:/analytics-lab`，`.gitignore` 已含 `*.csv` 和 `projects/*/data/`。
- 真实项目文档定位到桌面 KnowledgeVault：
  - `20-job/项目/项目选择.md` — 4 个项目选型总览
  - `20-job/项目/数据分析项目实战手册_v2.1.md` — 执行手册。**第 7 章（1003-1332 行）是 H&M 项目完整流程**：Step 0-1 分块读取清洗、Step 2 SQL RFM+同期群、Step 3 可视化、Step 4 README。第六章（958-1002）是电商词汇表。
- 手册铁律：所有数字是占位符 `[X]`，跑完真实数据再替换；做项目前先读对应知识包。

## 下一步计划

- 今天第一步：让杨淳栯跑"数据体检"代码（3 表各读前 5 行看列名结构），不碰 3.7GB 全量。
- 之后按手册 Step 0-1 做分块读取（chunksize=2_000_000，聚合到客户×月粒度）。
