# 2026-08-17 工作记录

## H&M 项目 1.1 阶段 A

- 用户开始自己写代码（第一次）。
- 卡点：`pd.read_csv(..., chunksize=100)` 返回 `TextFileReader`，用户不知道怎么从中取数据。
- 发现 bug：`usecols=[]` 传了空列表（等于读零列）。
- 已引导：① usecols 要么填 4 个列名、要么先删掉；② TextFileReader 是迭代器，用 `next()` 或翻书第6章「逐块读取」的 `for piece in chunker` 循环。
- 注意：用户深夜 00:13 仍在写代码，状态在线，但要注意别让他熬太晚。
- 用户对"翻书"有畏难（"不敢下手"），明确要求直接教工作常用写法。已给 `for chunk in reader` 循环 + `next(reader)` 两种读 chunk 写法；并提示 chunksize 别用 100（太碎），工作常用 200 万行一块。
- 观察到：用户对"迭代器/水龙头"这类抽象概念，用生活化类比（水龙头拧一下流一块）接收更快。
- **阶段 A 完成**：first chunk 读出 `(2000000, 4)`，usecols 4 列裁剪成功（sales_channel_id 已排除）。进入阶段 B（单块聚合）。
- 待确认：用户读入时是否加了 parse_dates（影响阶段 B 提取月份的方式）。
- 已确认：`t_dat` 当前类型是 `dtype('O')` = 字符串（object）。因为读入时**没加** `parse_dates`。
- 阶段 B 决策：走**字符串切片**路线（`chunk['t_dat'].str[:7]` → `'2018-09'`），不回头改 read_csv。优点：不改前面代码、立刻能跑通。
- `.str` 访问器书位置：第7章 7.4 String Manipulation。
- 阶段 B 第一步完成：`chunk['month'] = chunk['t_dat'].str[:7]` 提取月份成功（'2018-09' 格式）。观察到 chunk 跨 2018-09~2018-11（数据按时间排，200万行约 2-3 个月）。
- 进入阶段 B 第二步：groupby 聚合（4 指标：active_days/amount/items/last_date）。引导重点：active_days 用 nunique（同一天多件要去重），agg 传字典。
- 用户聚合函数理解盲区：① `nunique`（去重计数）不会；② 把 `max`（取最大值）误填到 article_id，搞混了 last_date 用 max 和 items 用 count。已给 4 指标×列×函数对照表理清。后续 RFM/留存还会反复用 nunique/count/max，注意他这几个函数概念不牢。
- **阶段 B 完成**：用户自己写出 groupby 聚合，4 指标正确（t_dat nunique/max、price sum、article_id count）。结果验证：00007d2d 跨 2018-09/2018-10 两行，说明客户×月分组生效。
- 剩收尾（明早）：列名是 MultiIndex（两层），customer_id/month 还在 index，需 reset_index + 压平重命名（active_days/last_date/amount/items），再进阶段 C。
- 今日整体：用户全程自己写代码（我只引导），从"不会读 chunk"到跑通聚合，01:00 收尾，正反馈扎实。
- 阶段 B 收尾完成：命名聚合 + reset_index 跑通，输出 6 列单层（customer_id/month/active_days/last_date/amount/items），格式对齐手册 Step 0-1。
- 进入阶段 C：循环跑全量 + concat + 二次聚合。引导重点：为什么拼完要二次 groupby（同一客户同月跨 chunk）。用户 01:12 仍要求继续。
- 阶段 B 第二步完成：groupby 聚合**逻辑正确**（customer_id+month 分组，nunique/max/sum/count 全对），但产生 MultiIndex 两层列名 + customer_id/month 在 index。下一步教「命名聚合」`agg(新列名=('原列','函数'))` + `.reset_index()`，得到干净 6 列。
- 阶段 C 审查（11:35，上午回来继续）：发现 3 个问题——① **中文引号混用**（`‘nunique'`，复制/手打代码常见坑，会 SyntaxError）；② `article__id` 双下划线 typo（应为 article_id）；③ 思路偏差：想"加到 DataFrame"而非"列表 append + concat"。已引导改正。
- 阶段 C 循环 + concat 跑通（11:45）：`monthly.shape = (6689550, 6)`，3000 万行明细压成 669 万行"客户×月"表。
- 待验证：是否做了二次聚合。用 `monthly.duplicated(subset=['customer_id','month']).sum()` 检查"客户×月"重复行。
- duplicated 检查结果：**459209** 个「客户×月」重复——跨块重复确实发生（45.9 万行被 chunksize 切开）。
- 进入二次聚合：groupby(['customer_id','month']).agg(active_days/amount/items=sum, last_date=max)，预期 shape 约 623 万行（669万-45.9万）。
- 二次聚合审查：用户 2 个错误——① 中文句号 `。agg(` 混入（又见中文标点坑）；② **列名混淆**：二次聚合对象 monthly 已无 t_dat/price/article_id（阶段 B 已聚合成指标列），应聚合 active_days/last_date/amount/items 这些现有列。说明用户没意识到 monthly 已是聚合后的表。
- 二次聚合逻辑正确：结果 `6230341` 行 = 669万 - 45.9万重复，合并成功，指标正确。
- 待修 2 个收尾问题：① 没**赋值**（`monthly.groupby(...).agg(...)` 结果没存回 monthly，monthly 还是旧 669万）；② 没 reset_index（customer_id/month 还在 index，显示 4 列）。改后应 `monthly.shape = (6230341, 6)`。
- **1.1 完整完成**（12:46）：`monthly.shape = (6230341, 6)`，3000 万行 → 623 万行「客户×月」表。全程用户自己写代码完成（分块+裁剪+聚合+二次聚合），我只引导。这是项目最硬的工程部分。
- 进入 1.2：客户表年龄清洗（异常值置空 + pd.cut 分箱）。
- 1.2 审查 clean_age 函数：5 个错误——① **核心概念**：`apply` 的参数 `a` 是标量（单个年龄值），用户误以为是一行，写了 `a['age']`；② `isna = True` 是赋值不是判断；③ `else if` 应为 `elif`；④ `||` 应为 `or`；⑤ 缺 `return a`（正常值分支）。
- 观察：用户 Python 基础语法薄弱（elif/or/|| 混淆，可能受 C/Java 等语言影响），且对 `apply` 逐个作用元素的机制不熟。后续引导注意补 Python 基础。
- 用户 apply 调用方式也错：`age.groupby('customer_id').apply(clean_age, age['age'])` —— ① 不需要 groupby（这是元素级清洗，不是分组）；② apply 只传函数，不多传第二参数。正确：`age['age_clean'] = age['age'].apply(clean_age)`。已纠正。
- 用户 pd.cut 又传了整个 DataFrame 报错（"Input array must be 1 dimensional"）。**反复模式**：DataFrame vs Series/标量 的维度混淆（apply 那轮 a 是标量不是行、这轮传整表不是一列）。后续注意反复强调"一列 = Series = 一维"。
- **1.2 完成**（13:27）：年龄清洗 + 分箱跑通。缺失/异常占比 **1.16%**（合理，异常值少）；分箱分布 16-25 最多（41.2万）→ 年轻客群为主，符合 H&M 快时尚定位。
- 用户问验收代码含义，已解释 `isna().mean()`（缺失占比）和 `value_counts()`（分布计数）——本质是"每步处理后验证自己没做错"。
- **1.3 完成 + 里程碑 1 完整完成**（14:32）：两张表成功写入 `data/hm.db`（文件数据库）——monthly_sales(6230341 行)、customers(1371980 行)，验证通过。
- 里程碑 1（数据清洗与聚合）三步全部通过：1.1 分块+聚合、1.2 年龄清洗分箱、1.3 写入 SQLite。**全程用户自己写代码完成**。
- 下一步：里程碑 2 SQL 核心分析（第一步 2.1 RFM 三维指标）。
- 2.1 RFM SQL 审查：F/M 对，结构对。2 个错误——① `julianday(2020-09-30)` 没加引号，SQL 把 `2020-09-30` 当算术 `2020-9-30=1981`，导致 Recency=-245万巨大负数；② `julianday(last_date)` 少 `MAX()`（一个客户多个月，应 `MAX(last_date)`）。
- 观察：用户 SQL 基础需补——日期字面量要加引号、GROUP BY 里非分组列要用聚合函数（MAX/SUM）。
- 用户追问"1.1 不是已经 max 过 last_date 了吗，为什么 2.1 还要 MAX"——**聚合粒度混淆**。已讲清：1.1 的 last_date 是"客户某月最后一天"（月粒度），2.1 要"客户所有月里最后一天"（客户粒度），所以要再 MAX。关键教学点：聚合是分层的，每层粒度不同，后续 2.2 同期群/2.5 沉睡还会遇到多层聚合。
- **2.1 RFM 完成**（14:53）：Recency 改对（0-730 正常范围），Frequency/Monetary 正确。RFM 三指标算出。
- 计划调整：回看手册，RFM 完整闭环还差「打分（NTILE 五分位）+ 分层（CASE WHEN 八类）」两步，先补这两步再做同期群。之前拆解遗漏了。
- rfm_base 表建好（验证通过：4 列 customer_id/recency_days/frequency/monetary）。
- 用户踩了 DROP+CREATE 拼一起报 syntax error（conn.execute 默认只能执行一条 SQL，多语句要用 `executescript`）。轻提。
- **NTILE 打分完成**（20:37）：验证1 分布均匀（5 档各约 27.2 万）；验证2 方向对（R_score=5 → recency_days 41~42 天，很小）。
- 观察：最活跃客户 recency_days 最小 41 天 → 数据实际最后交易约 2020-08-20，并非到 9-30（后续 README 局限可提）。
- 进入 2.1c：RFM 分层（CASE WHEN 八类）。
- **RFM 完整闭环完成**（20:58）：8 类全齐，总数 136 万。流失 59.5万(44%)、重要价值 33.7万(25%)、其余 43万(31%)。核心洞察：客户两极分化，留存是生死线。
- **进度快照（8-17 晚）**：已完成 里程碑0、里程碑1 全部 + 2.1 RFM 完整闭环（指标+打分+分层）。剩余：2.2 同期群（最复杂）、2.3 复购结构、2.4 品类帕累托（需 JOIN articles）、2.5 沉睡客群、里程碑3 可视化（3图，seaborn 未碰过）、里程碑4 README+GitHub发布。
- 工作量评估：今天完成约 40%（最难工程部分 3.7GB 分块已过）。剩余约 1~1.5 天。明天做完分析+可视化偏紧但可能；收尾发布留后天半天更现实。注意别给"明天必须做完"的压力（ADHD）。
- 追加（21:44）：用户今天额外自学了 SQL **窗口函数 + CASE 语句**（为 RFM 打分/分层补底层），学习投入度在线，但表示"好累"。
- 情绪节点：用户投的一个简历被拒，明确说"有点伤心"。这是求职受挫，可能触发自我否定。后续若继续投递或提到被拒，先接情绪、别空洞安慰，并帮他区分"被拒的是简历上还没项目的那版自己" vs "现在正在亲手把项目做出来的自己"。
